% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{polarisTSSRef}
\alias{polarisTSSRef}
\title{Gene annotation for POLARIS linkage}
\usage{
polarisTSSRef(genome = c("hg38", "mm10"))
}
\arguments{
\item{genome}{Either \code{"hg38"} or \code{"mm10"}, naming one of the slim reference
tables bundled with the package.}
}
\value{
A stranded \code{GRanges} with \code{gene_name} and \code{gene_type} columns.
}
\description{
Returns a \code{GRanges} of gene bodies with a \code{gene_name} column and strand,
suitable for the \code{gene.chr.ref} argument of \code{\link[=Polaris]{Polaris()}} and the \code{gene.anno}
argument of \code{\link[=SkymapLinkageTable]{SkymapLinkageTable()}}.
}
\details{
\strong{Prefer the annotation you already have.} The annotation must match the
genome build the peaks were called against, and a Signac ATAC assay already
carries exactly the right object:

\if{html}{\out{<div class="sourceCode">}}\preformatted{anno <- Signac::Annotation(obj[["ATAC"]])
}\if{html}{\out{</div>}}

That is guaranteed consistent with your peaks, which a bundled table is not.
Use \code{polarisTSSRef()} when no annotation is attached, and
\code{\link[=polarisMakeTSSRef]{polarisMakeTSSRef()}} for any build the package does not bundle.

The bundled tables carry only the columns POLARIS reads: gene name, sequence
name, TSS-bearing coordinate, strand and gene type. They are derived by
\code{data-raw/make_tss_ref.R}, which records their provenance.
}
\examples{
anno <- polarisTSSRef("hg38")
head(anno)

}
\seealso{
\code{\link[=polarisMakeTSSRef]{polarisMakeTSSRef()}}
}
