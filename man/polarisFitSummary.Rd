% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{polarisFitSummary}
\alias{polarisFitSummary}
\title{Tabulate the settings and selected ranks of one or more fits}
\usage{
polarisFitSummary(...)
}
\arguments{
\item{...}{\link{skymap} objects, or a single named \code{list} of them. Names become
the \code{dataset} column; unnamed fits are numbered.}
}
\value{
A \code{data.frame}, one row per fit: \code{dataset}, \code{n.cells},
\code{n.features.X}, \code{n.features.Y}, \code{T1}, \code{T2}, \code{T3}, \code{r1}, \code{r2}, \code{r3}, \code{r4},
\code{r5}, \code{x.sds}, \code{y.sds}, \code{seed}, \code{polaris.version}.
}
\description{
One row per fit, giving the data dimensions, the thresholds supplied and the
ranks the denoiser actually selected. This is the table the POLARIS
manuscript reports per dataset.
}
\details{
\code{r1} and \code{r2} are the modality ranks after being set to their common minimum;
\code{r1.auto} and \code{r2.auto}, the values the ratio rule chose before that, are kept
in \code{input.param} but not tabulated here.

Fits produced by the pre-package script will show \code{NA} for the ranks, because
that version captured \code{input.param} before the defaults were resolved. Recover
those with \code{ncol(fit$skymap.U)} and \code{ncol(fit$skymap.P)}.
}
\examples{
set.seed(1)
a <- Polaris(polarisSimulate(n = 150, g = 50, p = 60, r = 4)$X,
             polarisSimulate(n = 150, g = 50, p = 60, r = 4)$Y,
             x.sds = 0, verbose = FALSE)
polarisFitSummary(pbmc = a)

}
\seealso{
\code{\link[=polarisVersions]{polarisVersions()}}, \code{\link[=polarisSpectrum]{polarisSpectrum()}}
}
