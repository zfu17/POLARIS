% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbors.R
\name{SkymapFindNeighbors.Chr}
\alias{SkymapFindNeighbors.Chr}
\title{Cross-modality nearest neighbors within each chromosome}
\usage{
SkymapFindNeighbors.Chr(
  skymap,
  chr = "all",
  n.neighbors = 200L,
  metric = "Cosine",
  mc.cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{skymap}{A \link{skymap} fitted with \code{gene.chr.ref}.}

\item{chr}{Chromosomes to process, or \code{"all"}.}

\item{n.neighbors}{Neighbors per query.}

\item{metric}{Distance for the Annoy backend, e.g. \code{"Cosine"}.}

\item{mc.cores}{Cores, one chromosome per core.}

\item{verbose}{Report progress.}
}
\value{
The \code{skymap} with \code{nb.feature.chr}, a named list holding
\code{gene.to.feature} and \code{feature.to.gene} for each chromosome.
}
\description{
For every chromosome, finds each gene's nearest second-modality features and
each feature's nearest genes in the shared per-chromosome loading space.
}
\details{
Rewritten for the \code{list(P, Q, stdev)} layout that \code{Polaris()} stores. The
pre-package version indexed \code{skymap.feature.chr[[chr]]} as a single matrix
and called \code{rownames()} on it, which is \code{NULL} for a list, so it could not
run on any current fit.
}
