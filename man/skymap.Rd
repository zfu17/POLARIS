% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skymap-class.R
\name{skymap}
\alias{skymap}
\alias{skymap-class}
\title{The skymap object}
\description{
A \code{skymap} is the fitted POLARIS model returned by \code{\link[=Polaris]{Polaris()}}. It is a plain
named list carrying an S3 class attribute, so every element remains reachable
with \code{$} exactly as before this code became a package. Existing analysis
scripts that index a POLARIS fit by name continue to work unchanged; the class
only adds \code{print} and \code{summary} methods and lets other functions check that
they were handed a real fit.
}
\section{Elements}{

\describe{
\item{\code{skymap.cell}}{\code{n} x \code{(r3 + r4)} matrix. The joint cell embedding
\eqn{W = [\hat{U} | \hat{V}]}. Rows are cells, named by barcode.}
\item{\code{skymap.U}, \code{skymap.V}}{\code{n} x \code{r3} and \code{n} x \code{r4} matrices. The paired
cell embeddings. Their columns are matched one to one through the SVD,
which is what makes the per-cell concordance score meaningful.}
\item{\code{stdev.cell}}{singular values of the cell embedding.}
\item{\code{skymap.P}, \code{skymap.Q}}{\code{g} x \code{r5} and \code{p} x \code{r5} matrices. Genome-wide
gene and second-modality feature loadings.}
\item{\code{stdev.feature}}{singular-value weights \eqn{d} of the feature
embedding. These enter the linkage score.}
\item{\code{skymap.feature.chr}}{named list, one entry per chromosome, each
\code{list(P, Q, stdev)}. This is the per-chromosome decomposition that
\code{\link[=SkymapLinkageTable]{SkymapLinkageTable()}} scores, so every linkage is within-chromosome.}
\item{\code{X.svd}, \code{Y.svd}}{the truncated SVDs of the two denoised modalities.
Retained because \code{\link[=SkymapLinkageTable]{SkymapLinkageTable()}} reconstructs the denoised feature
vectors from them to compute the jackknife standard error.}
\item{\code{input.param}}{the thresholds supplied and the ranks actually
selected. Unlike earlier versions this records the resolved ranks, not
just the thresholds.}
}

Optional elements are added in place by downstream functions:
\code{concordance} and \verb{simscore.*} by \code{\link[=SkymapSimScore]{SkymapSimScore()}}, \verb{umap.*} by
\code{\link[=SkymapUMAP]{SkymapUMAP()}}, and \verb{nb.*} by \code{\link[=SkymapFindNeighbors]{SkymapFindNeighbors()}}.
}

