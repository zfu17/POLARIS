% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{polarisMakeTSSRef}
\alias{polarisMakeTSSRef}
\title{Build a gene annotation from a TxDb, EnsDb or GTF}
\usage{
polarisMakeTSSRef(x, gene.name.column = "gene_name")
}
\arguments{
\item{x}{One of: a \code{TxDb} or \code{EnsDb} object; a \code{GRanges} that already has
gene ranges; or a path to a GTF or GFF file.}

\item{gene.name.column}{For a \code{GRanges} or GTF input, the metadata column
holding gene symbols. Default \code{"gene_name"}, falling back to \code{"gene_id"}.}
}
\value{
A stranded \code{GRanges} with a \code{gene_name} column.
}
\description{
Turns any gene-level annotation source into the \code{GRanges} that POLARIS
expects, so the package is not limited to the builds it bundles.
}
\details{
Strand is essential. POLARIS places transcription start sites with
\code{GenomicRanges::resize(fix = "start")}, which is strand-aware, so an
unstranded annotation silently assigns minus-strand genes the wrong end of
the gene. This function errors if strand information is absent.
}
\examples{
\dontrun{
# From a Bioconductor annotation package
anno <- polarisMakeTSSRef(EnsDb.Hsapiens.v86::EnsDb.Hsapiens.v86)

# From the GTF that the reads were aligned against, which is the safest source
anno <- polarisMakeTSSRef("refdata-cellranger-arc-GRCh38/genes/genes.gtf.gz")
}

}
\seealso{
\code{\link[=polarisTSSRef]{polarisTSSRef()}}
}
