% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkage.R
\name{SkymapLinkageTable}
\alias{SkymapLinkageTable}
\title{Cis gene-peak linkage table with per-link standard errors}
\usage{
SkymapLinkageTable(
  skymap,
  gene.anno,
  window = 1e+06,
  se = TRUE,
  X = NULL,
  Y = NULL,
  tau2 = NULL,
  chrs = NULL,
  mc.cores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{skymap}{A \link{skymap} from \code{\link[=Polaris]{Polaris()}}, fitted with \code{gene.chr.ref} so that
it carries the per-chromosome feature embedding.}

\item{gene.anno}{A \code{GRanges} with a \code{gene_name} metadata column and strand,
used to place transcription start sites. See \code{\link[=polarisTSSRef]{polarisTSSRef()}}. Must be the
annotation for the genome build the peaks were called against.}

\item{window}{Half-width in bp of the cis window around the TSS. Peaks whose
\strong{midpoint} falls within \code{window} of the TSS are candidates. Default
\code{1e6}, the value used throughout the POLARIS manuscript.}

\item{se}{Compute per-link standard errors, and from them the
empirical-Bayes shrunken score and local false sign rate. Default \code{TRUE}.
Setting \code{FALSE} returns \code{score_raw} and \code{dist} only and is much faster.}

\item{X, Y}{Optional cells-by-features matrices. \strong{Supplying these changes the
estimator.} See Details. Leave them \code{NULL} to reproduce the published
standard errors.}

\item{tau2}{Empirical-Bayes prior variance. Estimated by moments from the
data when \code{NULL}.}

\item{chrs}{Restrict to these chromosomes. Default all present.}

\item{mc.cores}{Cores for the per-gene standard-error computation.}

\item{verbose}{Report progress.}
}
\value{
A \code{data.frame} sorted by decreasing \code{score_adj} (or \code{score_raw} when
\code{se = FALSE}), with columns \code{gene}, \code{peak}, \code{chr}, \code{dist}, \code{score_raw}, and
when \code{se = TRUE} also \code{SE}, \code{score_adj}, \code{p_lfsr}, \code{q_lfsr}.
}
\description{
Enumerates every within-chromosome gene-peak pair whose peak midpoint lies
within \code{window} of the gene's strand-aware transcription start site, scores
each pair by the singular-value-weighted inner product of its loadings, and
attaches a closed-form leave-one-cell-out standard error.
}
\details{
The raw linkage score is the singular-value-weighted inner product
\deqn{\mathrm{IPw}(g,p) = P_g^\top \mathrm{diag}(d) Q_p,}
computed within each chromosome, so no gene is ever scored against a peak on
another chromosome.

The standard error is a fixed-embedding leave-one-cell-out jackknife that
does not refit POLARIS. Writing the score as a bilinear form
\eqn{\mathrm{IPw} = x^\top K y} in the cell kernel \eqn{K = WW^\top}, all \code{n}
omitted-cell scores follow in closed form from two matrix-vector products,
and \eqn{\mathrm{SE} = \sqrt{\tfrac{n-1}{n}\sum_i (s_{(-i)} - \bar{s})^2}}.

\code{z_dev}, the genomic-distance-aware statistic that the POLARIS benchmarks
rank by, is not a column here. Compute it with \code{\link[=SkymapZdev]{SkymapZdev()}}, which needs
the whole table in order to form the within-distance-bin baselines.
}
\section{Which vectors the standard error uses}{

The POLARIS standard error is defined on the \strong{rank-\code{r} denoised} gene and
peak vectors. That is what you get by leaving \code{X} and \code{Y} as \code{NULL}: they are
reconstructed from the SVDs stored in the fit, and this is the estimator
behind every published POLARIS result.

If you pass \code{X} and \code{Y}, the standard error is instead computed on those raw
normalized matrices. That is a different estimator, it is not the one in the
manuscript, and it will change \code{SE}, \code{score_adj} and any \code{z_dev} derived from
them. The argument is retained for comparison only, and warns when used.
}

\examples{
\dontrun{
anno <- polarisTSSRef("hg38")
fit  <- Polaris(X, Y, gene.chr.ref = anno, mc.cores = 4)
tab  <- SkymapLinkageTable(fit, gene.anno = anno, mc.cores = 4)
tab  <- SkymapZdev(tab)
head(tab[order(-tab$z_dev), ])
}

}
\seealso{
\code{\link[=SkymapZdev]{SkymapZdev()}}, \code{\link[=polarisTSSRef]{polarisTSSRef()}}
}
