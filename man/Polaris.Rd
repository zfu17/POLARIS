% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polaris.R
\name{Polaris}
\alias{Polaris}
\title{Fit POLARIS to two paired modalities}
\usage{
Polaris(
  X,
  Y,
  T1 = 1.01,
  T2 = 1.01,
  T3 = 1.1,
  r1 = NULL,
  r2 = NULL,
  r3 = NULL,
  r4 = NULL,
  r5 = NULL,
  gene.chr.ref = NULL,
  chrs = NULL,
  x.sds = 0.95,
  y.sds = 0,
  mc.cores = 1L,
  seed = 1L,
  verbose = TRUE
)
}
\arguments{
\item{X}{Numeric matrix for modality 1, \strong{cells in rows, features in
columns}, centered and scaled. For scMultiome this is the RNA
\code{scale.data}, transposed.}

\item{Y}{Numeric matrix for modality 2, cells in rows, features in columns,
with the same cells in the same order as \code{X}. For scMultiome this is the
TF-IDF normalized ATAC \code{data}, transposed; for CITE-seq the CLR-normalized
ADT \code{data}, transposed.}

\item{T1, T2}{Singular-value ratio thresholds of the denoiser for \code{X} and \code{Y}.
Components are retained while the spectrum still decays and truncated once
it flattens. Defaults \code{1.01}.}

\item{T3}{Ratio threshold for the feature-embedding rank \code{r5}. Default \code{1.1}.}

\item{r1, r2}{Ranks retained for \code{X} and \code{Y}. Derived from \code{T1} and \code{T2} unless
given. Note that the two modality ranks are set to their common minimum, so
supplying unequal values has no effect beyond the smaller of the two.}

\item{r3, r4}{Dimensions of \eqn{\hat{U}} and \eqn{\hat{V}}. Default
\code{min(r1, r2)}.}

\item{r5}{Dimension of \code{P} and \code{Q}. Derived from \code{T3} unless given.}

\item{gene.chr.ref}{A \code{GRanges} with a \code{gene_name} metadata column and
strand, giving the genomic location of the features of \code{X}. Required for the
per-chromosome feature decomposition that \code{\link[=SkymapLinkageTable]{SkymapLinkageTable()}} scores. If
\code{NULL} the per-chromosome step is skipped with a message and the fit still
returns the genome-wide \code{skymap.P} and \code{skymap.Q}, which is the right
behaviour for modality pairs without genomic coordinates such as CITE-seq.
See \code{\link[=polarisTSSRef]{polarisTSSRef()}}.}

\item{chrs}{Chromosomes to decompose. Default \code{NULL} uses those actually
present in both \code{gene.chr.ref} and the feature names of \code{Y}.}

\item{x.sds, y.sds}{Standard-deviation floors for retaining features of \code{X}
and \code{Y}. A feature is kept when its standard deviation exceeds the floor.
The defaults are deliberately asymmetric: \code{x.sds = 0.95} keeps genes that
retain appreciable variance, which for globally scaled data subset to one
cell population is a broader and more permissive selection than a
highly-variable-gene list, while \code{y.sds = 0} removes only constant features
of the second modality. Set \code{x.sds = 0} for modality pairs where \code{X} is not
z-scored, as the CITE-seq analyses do.}

\item{mc.cores}{Cores for the per-chromosome decomposition.}

\item{seed}{Seed for the starting vector of the \code{X} decomposition. The
truncated SVD used for \code{X} draws a random start, so without a fixed seed
two fits of the same data differ slightly (about \code{1e-9} in the singular
values) and the caller's random stream is advanced as a side effect.
POLARIS therefore fixes the seed locally and restores the previous RNG state
afterwards, so a fit is bit-reproducible and leaves your random stream
untouched. Pass \code{NULL} for the older non-deterministic behaviour.}

\item{verbose}{Report progress.}
}
\value{
A \link{skymap} object.
}
\description{
Performs the POLARIS spectral integration of two matrices measured on the
same cells and returns a \link{skymap} object carrying the joint cell embedding,
the paired cell embeddings used for the concordance score, and the cross-modal
feature embedding.
}
\details{
The optimizer of the POLARIS objective is obtained from the SVD of
\eqn{(XX^\top)^{1/2}(YY^\top)^{1/2}}. Rather than forming those \code{n} x \code{n}
operators, this implementation uses the exact identity
\eqn{XX^\top YY^\top = U_1 C U_2^\top}, where \eqn{U_1} and \eqn{U_2} are the
cell-space singular vectors of the two denoised modalities, reducing the
problem to an \code{r} x \code{r} SVD. The feature-loading product is likewise
decomposed through its thin QR factors instead of materializing a dense
genes-by-features matrix. Both give the identical decomposition.

Ranks are selected by the ratio rule
\eqn{k = \max\{j : \sigma_j / \sigma_{j+1} > T\}}.
}
\examples{
set.seed(1)
sim <- polarisSimulate(n = 200, g = 60, p = 80, r = 5)
fit <- Polaris(sim$X, sim$Y, x.sds = 0, verbose = FALSE)
fit

}
\seealso{
\code{\link[=SkymapSimScore]{SkymapSimScore()}}, \code{\link[=SkymapLinkageTable]{SkymapLinkageTable()}}
}
