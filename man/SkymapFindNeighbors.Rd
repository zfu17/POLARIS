% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbors.R
\name{SkymapFindNeighbors}
\alias{SkymapFindNeighbors}
\title{Nearest-neighbor graphs on skymap embeddings}
\usage{
SkymapFindNeighbors(
  skymap,
  slot = "skymap.cell",
  n.neighbors = 300L,
  metric = "Cosine",
  mc.cores = 1L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{skymap}{A \link{skymap} from \code{\link[=Polaris]{Polaris()}}.}

\item{slot}{Which embedding(s) to build a graph on. One or more of
\code{"skymap.cell"}, \code{"skymap.U"}, \code{"skymap.V"}, \code{"skymap.P"}, \code{"skymap.Q"}, or
\code{"all"}. Defaults to \code{"skymap.cell"}.}

\item{n.neighbors}{Neighbors per point.}

\item{metric}{Distance for the Annoy backend. Note the capitalized spelling
BiocNeighbors expects, e.g. \code{"Cosine"}, which differs from uwot's
\code{"cosine"} in \code{\link[=SkymapUMAP]{SkymapUMAP()}}.}

\item{mc.cores}{Cores, one slot per core.}

\item{verbose}{Report progress.}

\item{...}{Further arguments for \code{\link[BiocNeighbors:findKNN]{BiocNeighbors::findKNN()}}.}
}
\value{
The \code{skymap} with an \verb{nb.<slot>} list added per slot, each carrying
\code{index} and \code{distance} matrices with rownames taken from the embedding.
}
\description{
Nearest-neighbor graphs on skymap embeddings
}
\examples{
set.seed(1)
sim <- polarisSimulate(n = 150, g = 50, p = 60, r = 4)
fit <- Polaris(sim$X, sim$Y, x.sds = 0, verbose = FALSE)
fit <- SkymapFindNeighbors(fit, n.neighbors = 10, verbose = FALSE)
dim(fit$nb.skymap.cell$index)

}
