% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{polarisVersions}
\alias{polarisVersions}
\title{Versions of POLARIS and its dependencies}
\usage{
polarisVersions(which = c("imports", "all"), installed.only = TRUE)
}
\arguments{
\item{which}{\code{"imports"} (default) reports POLARIS, R and everything in the
\code{Imports} field. \code{"all"} adds the \code{Suggests} packages that are installed.}

\item{installed.only}{Drop rows for packages that are not installed. Default
\code{TRUE}.}
}
\value{
A \code{data.frame} with columns \code{package} and \code{version}, POLARIS first,
then R, then dependencies alphabetically.
}
\description{
Reports the version of POLARIS, of R, and of the packages POLARIS depends on,
in a form that can be pasted straight into a manuscript software table.
}
\details{
Every fit also records this at the time it ran, in
\code{skymap$input.param$versions}, so a saved \code{skymap} documents the software that
produced it even if the environment later changes. \code{\link[=polarisFitSummary]{polarisFitSummary()}}
surfaces that per fit.
}
\examples{
polarisVersions()

}
\seealso{
\code{\link[=polarisFitSummary]{polarisFitSummary()}}
}
