% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{polarisSimulate}
\alias{polarisSimulate}
\title{Simulate a paired two-modality dataset}
\usage{
polarisSimulate(
  n = 300,
  g = 80,
  p = 120,
  r = 5,
  noise = 0.5,
  discordant = 0.2,
  feature.names = TRUE
)
}
\arguments{
\item{n}{Number of cells.}

\item{g}{Number of features in modality 1.}

\item{p}{Number of features in modality 2.}

\item{r}{Number of shared latent factors.}

\item{noise}{Standard deviation of the independent noise added to each
modality.}

\item{discordant}{Fraction of cells whose second modality is driven by
private rather than shared factors. These are the low-concordance cells.}

\item{feature.names}{Whether to name modality-2 features as genomic
intervals (\code{chr1-100-600}, ...) so the simulation can be passed through
\code{\link[=SkymapLinkageTable]{SkymapLinkageTable()}}. Default \code{TRUE}.}
}
\value{
A list with
\describe{
\item{\code{X}, \code{Y}}{cells-by-features matrices, centered and scaled.}
\item{\code{Z}}{the shared latent factors, \code{n} x \code{r}.}
\item{\code{shared}}{per-cell weight on the shared factors, in \verb{[0,1]}. Cells
with a low value should receive a low concordance score.}
\item{\code{anno}}{a \code{GRanges} placing the modality-1 features on \code{chr1}, so
the simulation also exercises the linkage path.}
}
}
\description{
Generates two matrices on the same cells that share a low-rank latent
structure, with a per-cell weight controlling how strongly each cell's
second modality reflects the shared factors. Cells with a low weight are
cross-modality discordant by construction, so the simulation gives a ground
truth for the concordance score.
}
\details{
Intended for examples, tests and teaching rather than for benchmarking.
}
\examples{
set.seed(1)
sim <- polarisSimulate(n = 200, g = 60, p = 80, r = 5)
fit <- SkymapSimScore(Polaris(sim$X, sim$Y, x.sds = 0, verbose = FALSE))
# discordant cells should score lower
tapply(fit$concordance, sim$shared < 0.5, mean)

}
