% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reductions.R
\name{SkymapUMAP.Chr}
\alias{SkymapUMAP.Chr}
\title{UMAP of the per-chromosome feature embedding}
\usage{
SkymapUMAP.Chr(
  skymap,
  chr = "all",
  n_components = 2L,
  n_neighbors = 30L,
  metric = "cosine",
  min_dist = 0.2,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{skymap}{A \link{skymap} fitted with \code{gene.chr.ref}.}

\item{chr}{Chromosomes to reduce, or \code{"all"}.}

\item{n_components, n_neighbors, metric, min_dist}{Passed to \code{\link[uwot:umap]{uwot::umap()}}.}

\item{verbose}{Report progress.}

\item{...}{Further arguments for \code{\link[uwot:umap]{uwot::umap()}}.}
}
\value{
The \code{skymap} with \code{umap.feature.chr}, a named list of
\code{data.frame}s each carrying a \code{feature_type} column marking rows as \code{gene}
or \code{feature}.
}
\description{
Reduces the gene and second-modality loadings of one or more chromosomes,
stacking the two feature types into a single UMAP per chromosome so genes and
peaks share a coordinate system.
}
\details{
This function previously assumed \code{skymap.feature.chr[[chr]]} was a single
combined matrix. \code{Polaris()} stores \code{list(P, Q, stdev)} per chromosome, so
the loadings are stacked here explicitly.
}
