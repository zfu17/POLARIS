% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reductions.R
\name{SkymapUMAP}
\alias{SkymapUMAP}
\title{UMAP of a skymap embedding}
\usage{
SkymapUMAP(
  skymap,
  slot = "skymap.cell",
  n_components = 3L,
  n_neighbors = 30L,
  metric = "cosine",
  min_dist = 0.2,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{skymap}{A \link{skymap} from \code{\link[=Polaris]{Polaris()}}.}

\item{slot}{Which embedding(s) to reduce. One or more of \code{"skymap.cell"},
\code{"skymap.U"}, \code{"skymap.V"}, \code{"skymap.P"}, \code{"skymap.Q"}, or \code{"all"}.
Defaults to \code{"skymap.cell"}, the joint cell embedding.}

\item{n_components, n_neighbors, metric, min_dist}{Passed to \code{\link[uwot:umap]{uwot::umap()}}.
Note \code{metric} here takes uwot's lowercase spelling, e.g. \code{"cosine"}.}

\item{verbose}{Report progress.}

\item{...}{Further arguments for \code{\link[uwot:umap]{uwot::umap()}}.}
}
\value{
The \code{skymap} with results added under \code{skymap$UMAPs}, one
\code{data.frame} per slot named \verb{<slot>.UMAP}. Repeated calls accumulate rather
than replace, which is the storage contract the analysis scripts expect
(for example \code{skymap$UMAPs$skymap.cell.UMAP}).
}
\description{
UMAP of a skymap embedding
}
\examples{
set.seed(1)
sim <- polarisSimulate(n = 150, g = 50, p = 60, r = 4)
fit <- Polaris(sim$X, sim$Y, x.sds = 0, verbose = FALSE)
fit <- SkymapUMAP(fit, n_neighbors = 15, verbose = FALSE)
head(fit$UMAPs$skymap.cell.UMAP)

}
